
  # Create New Repository

  This is a code bundle for Create New Repository. The original project is available at https://www.figma.com/design/IfGtMbc1SjlH9C8t5WroLG/Create-New-Repository.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  